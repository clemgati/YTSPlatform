<!--
  Console only, and deliberately so: there is nowhere to ship logs to yet, and a file
  appender on a machine nobody is running is a file nobody reads.

  Ktor and Netty are quietened to INFO because their DEBUG is a wall of frame chatter that
  buries the two things this server actually says — a failed mail send, and a migration.
-->
<configuration>
    <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>%d{HH:mm:ss.SSS} %-5level %logger{24} - %msg%n</pattern>
        </encoder>
    </appender>

    <logger name="io.ktor" level="INFO"/>
    <logger name="io.netty" level="INFO"/>
    <logger name="org.flywaydb" level="INFO"/>
    <logger name="com.zaxxer.hikari" level="INFO"/>

    <root level="INFO">
        <appender-ref ref="CONSOLE"/>
    </root>
</configuration>
